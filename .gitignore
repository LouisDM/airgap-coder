# ── 密钥，绝不提交 ──────────────────────────────────
.env

# 站点特定的上游注册表（由 lc 生成）。它本身不含地址也不含密钥——那些只在 .env
# 里，registry 只存变量名。issue #4 计划把它改成可提交（团队共享结构定义），
# 但 ci.yml 里还有一条「registry.json 必须被 gitignore」的断言，得先一起改。
registry.json

# ── 生成物 ────────────────────────────────────────
# codex/ 是 CODEX_HOME，内容全部由 `lc sync` 生成或 Codex 运行时产生
# （config.toml、profile、以及 sqlite 会话库 / installation_id / skills）。
# 会话库可能含代码片段与 prompt，尤其不能入库。
codex/

litellm/config.yaml

# lc export 的产物：GB 级，且是搬运件而不是源码，不进版本库
airgap-coder-*.tar.gz

# 测试沙箱
.smoke/

.DS_Store
__pycache__/
