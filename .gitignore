# ── 密钥，绝不提交 ──────────────────────────────────
.env

# registry.json 刻意不在这里：issue #4 之后它只存结构定义与变量名，地址和密钥
# 都在 .env 里。团队把它提交上来，新人 clone 后只需写自己的 .env，不用重跑一遍
# lc init 逐项填 model / context window / 后端类型。
# ci.yml 里有一条断言守着这个前提：registry.json 里出现任何 URL 或疑似密钥就变红。

# ── 生成物 ────────────────────────────────────────
# codex/ 是 CODEX_HOME，内容全部由 `lc sync` 生成或 Codex 运行时产生
# （config.toml、profile、以及 sqlite 会话库 / installation_id / skills）。
# 会话库可能含代码片段与 prompt，尤其不能入库。
codex/

litellm/config.yaml

# lc export 的产物：GB 级，且是搬运件而不是源码，不进版本库
airgap-coder-*.tar.gz

# 测试沙箱
.smoke/
.codex-review/

.DS_Store
__pycache__/
